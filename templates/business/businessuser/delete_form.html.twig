<form method="post" action="{{ path('delete_user', {'guid': businessUser.guid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ businessUser.guid) }}">
    <button class="btn">Delete</button>
</form>
