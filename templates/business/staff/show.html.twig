{% extends 'business/staff/base.html.twig' %}

{% block title %}Staff{% endblock %}

{% block body %}
    <h1>Staff</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ staff.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ staff.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ staff.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_staff_index') }}">back to list</a>

    <a href="{{ path('app_staff_edit', {'guid': staff.guid}) }}">Edit</a>

    {{ include('business/staff/_delete_form.html.twig') }}
{% endblock %}
